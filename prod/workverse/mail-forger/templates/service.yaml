apiVersion: v1
kind: Service
metadata:
  name: {{ include "mail-forger.fullname" . }}
  labels:
    {{- include "mail-forger.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mail-forger.selectorLabels" . | nindent 4 }}
