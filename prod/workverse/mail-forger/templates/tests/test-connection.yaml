apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mail-forger.fullname" . }}-test-connection"
  labels:
    {{- include "mail-forger.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mail-forger.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
