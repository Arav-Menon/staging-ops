apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "space.fullname" . }}-test-connection"
  labels:
    {{- include "space.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "space.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
